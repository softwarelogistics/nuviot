#ifndef FLOWMETER_H
#define FLOWMETER_H

#include <Arduino.h>

public class FlowMeter {
    private:
        uint8_t m_pin;
    
    public:
        FlowMeter(uint8_t pin) {
            m_pin = pin;
        }

        setup() {

        }

        loop() {

            
        }
};

#endif